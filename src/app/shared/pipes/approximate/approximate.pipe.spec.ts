import { ApproximatePipe } from './approximate.pipe';

describe('ApproximatePipe', () => {
  it('create an instance', () => {
    const pipe = new ApproximatePipe();
    expect(pipe).toBeTruthy();
  });
});
